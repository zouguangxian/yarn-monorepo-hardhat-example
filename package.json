{
  "name": "yarn-monorepo-hardhat",
  "packageManager": "yarn@3.3.1",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/config/*"
  ],
  "scripts": {
    "build": "$npm_execpath workspaces foreach run build"
  },
  "devDependencies": {
    "hardhat": "^2.12.7"
  }
}
